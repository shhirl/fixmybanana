# 2026-09-08 after — live site once PRs #9 and #10 deployed (tag `look-2026-09-08-how-its-built`)

Captured from www.fixmybanana.com with headless Chrome at 19:00 CEST.

| File | What it shows |
|---|---|
| `home-desktop-top.png` | `/` unchanged except a small top-right "How it's built →" link inside the card and a "How it's built" footer link (replacing the duplicate whirleyworld.com link). |
| `result-desktop-top.png` | `/upload` result for the demo photo: verdict card, then the new line "Evaluated on 54 photos · agrees with my label 60% of the time · see how", then the photo. |
| `how-its-built-desktop-full.png` | The whole `/how-its-built` page at desktop width (scaled to 2/3). |
| `how-its-built-mobile-top.png` | Top of `/how-its-built` at 390 px: nav wraps, no horizontal overflow. |

Compare with `../2026-09-08-baseline/` (before).
